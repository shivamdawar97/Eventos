<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.shivam97.eventos">

    <application
        android:name=".Eventos"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".addeventsclasses.OrganisersList"></activity>
        <activity android:name=".addeventsclasses.SelectOrganisers" />
        <activity android:name=".addeventsclasses.OrganizersListActivity" />
        <activity
            android:name=".mainFragments.MainActivity"
            android:exported="true"
            android:label="@string/app_name" />
        <activity android:name=".eventClasses.EventDetails" />
        <activity
            android:name=".addeventsclasses.AddEvent"
            android:exported="true" />
        <activity
            android:name=".logInClasses.LogIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".logInClasses.AccSetup"
            android:exported="true" />
    </application>

</manifest>